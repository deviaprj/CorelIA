{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "corelia-api",
  "main": "src/index.ts",
  "compatibility_date": "2026-01-01",
  "workers_dev": true,
  "observability": {
    "enabled": true
  },

  // Route Worker sur la zone zentic.fr (déjà sur Cloudflare).
  // `api.zentic.fr` est couvert par un enregistrement de la zone : la route
  // rattache le Worker au hostname sans avoir à créer d'enregistrement DNS.
  // La zone héberge déjà d'autres routes (nei.zentic.fr) — elles sont conservées.
  "routes": [
    {
      "pattern": "api.zentic.fr/*",
      "zone_name": "zentic.fr"
    }
  ],

  // Workers AI (repli texte/vision) — aucune clé requise, facturé au neurone.
  "ai": {
    "binding": "AI"
  },

  "vars": {
    "ALLOWED_ORIGINS": "https://zentic.fr,https://www.zentic.fr",
    "AI_TEXT_MODEL": "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
    // Repli vision (utilisé uniquement sans clé DeepSeek) : Mistral Small 3.1
    // est multimodal (Apache-2.0) et accepte le format `messages` d'OpenAI.
    // LLaVA est écarté (déprécié : ne répond plus) et les modèles Meta aussi
    // (leur licence interdit explicitement l'Union européenne).
    "AI_VISION_MODEL": "@cf/mistralai/mistral-small-3.1-24b-instruct",
    "RATE_LIMIT_PER_MINUTE": "100"
  }

  // Secrets à définir (jamais dans ce fichier) :
  //   npx wrangler secret put DEEPSEEK_API_KEY   # clé du fournisseur IA
  //   npx wrangler secret put CLIENT_API_KEY     # clé publique de l'app (soft gate)
  //
  // Variable optionnelle :
  //   DEEPSEEK_VISION_MODEL  # modèle multimodal DeepSeek (défaut : deepseek-chat)
}
